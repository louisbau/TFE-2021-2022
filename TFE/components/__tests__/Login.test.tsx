import React from "react";
import renderer from "react-test-renderer";
import App from "../../App"

describe('<App />', () => {
  it("has 1 child", () => {
    
    // @ts-ignore
    expect(1).toBe(1);
  });
});
